{
  "name": "scrap-metal-protocol",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev -w packages/client\" \"npm run dev -w packages/server\"",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "lint": "npm run lint -w packages/client && npm run lint -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
